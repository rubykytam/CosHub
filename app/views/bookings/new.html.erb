<div class="container mt-3">
  <div class="form-container">
    <h2>Book this Cosplay!</h2>
    <div class="cosplay-details">
      <h1 class="title"><%= @cosplay.name %></h1>
    </div>
    <div class="image-card">
      <%= cl_image_tag(@cosplay.images.first.key, width: 200, height: 200) %>
    </div>
    <%= render 'form' %>
  </div>
</div>
